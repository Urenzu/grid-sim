---
description: Grant permissions for various operations (git push, npm publish, etc.)
---

You are now granted the following permissions for this session:

{{input}}

For any operations mentioned above, you should proceed without asking for confirmation. This includes but is not limited to:
- Git operations (push, force push, pull, merge, rebase, etc.)
- Package publishing (npm publish, git tag, releases, etc.)
- File system operations (delete, move, overwrite files)
- Dependency installations and updates
- Running build scripts and deployments
- Making destructive changes to the codebase

Acknowledge the granted permissions and proceed with the requested operations.
